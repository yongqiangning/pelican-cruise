# 把「鹈鹕骑单车」装成 Chrome / Edge 浏览器插件（新标签页）

装完这个插件后，**每次新建标签页就是那只鹈鹕在海岸公路上骑车**——清晨光线、跟随镜头、6 km/h 定速巡航，画面和桌面动态壁纸是同一套。

在壁纸那套纯画面的基础上，插件多两层东西（壁纸版都没有：桌面上那个窗口连鼠标都不属于它）：

- **一层桌面信息**：正上方的大时钟（带星期和农历）、搜索框、可自由增删的快捷网址。**右上角有一片树叶，点一下把这层全收起来、只留画面，再点一下放回来**（状态会记住，快捷键 <kbd>L</kbd>）。
- **一条底部控制条**：拖滑块调速、拖「海浪」滑块调环境音音量、点按钮切时段、跳跃 / 特技 / 车铃 / 大叫、换镜头、开关声音，最右边还有一个 <kbd>⌨</kbd> **快捷键** 按钮，鼠标移上去就浮出一张快捷键表；键盘 <kbd>W</kbd><kbd>S</kbd><kbd>A</kbd><kbd>D</kbd>、<kbd>空格</kbd>、<kbd>T</kbd> 也都能用。不想要就在配置里关掉。

两层都能单独关闭（见第 6 节），关掉任意一层就退回纯画面。

整份插件是离线自包含的（约 869 KB，含 4 个尺寸图标），**安装时不申请任何浏览器权限**（唯一一个「读取网页」是可选权限，只在你在弹窗里点「查找高清图标」时才申请，见 4.3），不读取你的浏览内容、历史记录或书签。

---

## 1. 先拿到插件文件

仓库只存源码，`dist/` 不入库，所以要自己构建一次：

```powershell
cd pelican-bike
npm install --no-package-lock --registry=https://registry.npmmirror.com
node build.mjs
```

构建完会得到这几个产物：

| 文件 / 目录 | 用途 |
| --- | --- |
| `dist/index.html` | 正常游戏页面（带界面、能操作） |
| `dist/wallpaper.html` | 桌面动态壁纸用（界面全隐藏） |
| **`dist/extension/`** | **浏览器插件本体**，装它 |
| `dist/pelican-newtab-extension.zip` | 上面这个目录的压缩包，方便拷到别的机器 |

> 也可以先双击 `dist/extension/newtab.html`、用 Chrome 打开看看效果再决定装不装。
> 注意：直接双击打开时是 `file://` 页面，脚本能跑、画面正常，但那只代表画面本身没问题——真正装成插件后走的是 `chrome-extension://`，功能一致。（`file://` 下「记住收起状态」用的是那个目录的 localStorage，和插件里的数据互不相通。）

---

## 2. 安装（Chrome / Edge 都一样）

1. 地址栏输入 `chrome://extensions`（Edge 用 `edge://extensions`）回车
2. 打开右上角的 **开发者模式**
3. 点左上角 **加载已解压的扩展程序**
4. 选中 **`dist/extension`** 这个**文件夹**（不是里面的某个文件）
5. 完成。按 <kbd>Ctrl</kbd>+<kbd>T</kbd> 新建一个标签页看看

> 如果是从别的机器拷过来的：把 `pelican-newtab-extension.zip` 解压，然后照上面第 4 步选中解压出来的 `extension` 文件夹即可。
> Chrome 会提示这个插件「替换新建标签页」，这是 `chrome_url_overrides.newtab` 的固定提示，属正常。

**卸载 / 换回原来的新标签页**：到 `chrome://extensions` 把「鹈鹕骑单车 · 新标签页」关掉或移除，新标签页立刻恢复原样。

---

## 3. 它和桌面壁纸版的区别

| | 桌面壁纸 `wallpaper.html` | 新标签页插件 |
| --- | --- | --- |
| 什么时候出现 | 一直在桌面背景（被全屏应用挡住时自动暂停） | 打开新标签页时 |
| 文件形态 | 单个 HTML，拖进 Lively 即可 | 一个扩展目录，装进浏览器 |
| 画面与参数 | 完全一样（同一份源码、同一套配置） | 完全一样 |
| 桌面层（时钟 / 搜索 / 快捷网址） | 没有 | **有**，右上角树叶一键收起 |
| 能不能操作 | 不能。鼠标键盘都不响应，也不会出声 | **能**。底部控制条 + 键盘，声音默认关、点了才开；环境音音量拖控制条上的「海浪」滑块 |
| 配置位置 | `wallpaper.html` 里搜 `__PELICAN_WP` | `dist/extension/config.js` |

---

## 4. 桌面层：时钟 / 搜索 / 快捷网址

这三样只属于插件版。壁纸版和游戏版里连对应的代码和样式都不会出现（构建时就没往里放）。

### 4.1 时钟

正上方，24 小时制，下面一行是「9月25日 星期五 八月十五」——公历 + 星期 + **农历**。农历直接用浏览器内置的中国历法（`Intl` 的 `chinese` 日历）算，不依赖任何外部接口，也不会因为闰月、大小月而出错。

### 4.2 搜索框

- 直接输入关键词回车 → 用当前搜索引擎搜
- **输入网址会自动识别并直接打开**（`github.com`、`https://…`、`localhost:3000` 都行），不会傻乎乎地拿去搜
- 点搜索框左侧那个小图标 → 切换搜索引擎（必应 / 百度 / Google / 搜狗 / 知乎 / B 站），选完记住
- 按 <kbd>/</kbd> 可以直接把光标放进搜索框

### 4.3 快捷网址

- 首次打开预置了 12 个常用站点（百度、B 站、知乎、小红书、淘宝、微博、抖音、豆瓣、GitHub、YouTube、DeepSeek、通义千问），之后完全由你增删
- **添加**：点格子末尾的 `+`，填名称和网址（`zhihu.com` 这种不带协议的也行，会自动补 `https://`）
- **修改 / 删除**：鼠标移到格子上，右上角出现铅笔和叉；删掉之后底部会留一个「撤销」，**8 秒内都能反悔**
- **排序**：直接拖拽格子换位置
- 所有改动存在浏览器本地，关掉标签页也不会丢

**图标是怎么来的**：留空时自动帮你找**最清晰的那张**，按四级依次来，拿到 180px 以上就收手：

1. **你手动填的**（emoji 或图片链接）——填了就只用你填的，不猜
2. **预置的高清地址**：12 个默认站点各自记了官方 `apple-touch-icon` 一类的原图（百度 1024、B 站 512、DeepSeek 180……）
3. **本地缓存**：查到过的结果按域名存下来，30 天内不再重复探测
4. **猜站点根目录的常见路径**：`apple-touch-icon.png` 及各种尺寸变体、`android-chrome-192/512`、`favicon.svg`、`favicon-196x196.png`、`mstile-150x150.png`、`favicon.ico` 等十几条；**并排加载完再比像素**，不是「谁先成功算谁」，所以不会再出现 32px 的 `favicon.ico` 抢在 180px 的 `apple-touch-icon` 前面

都没到 180px 的话，弹窗里会出现 **「查找高清图标」** 按钮：点它授权一次「读取网页」，插件就会读一次那个站点的首页，把它自己声明的图标找出来（`<link rel="apple-touch-icon">`、PWA `manifest.json` 里的 192/512 图标、`msapplication-TileImage`，最后才轮到 `og:image`）——放 CDN 上的原图往往只有这一条路能拿到，比如 DeepSeek 的图标在 `fe-static.deepseek.com`，根目录什么都没有。授权是**可选权限**，安装时不申请，点了才申请，拒绝也不影响其它功能。

实在找不到就退成**名称首字的彩色方块**，颜色由域名算出来，同一个站永远是同一个颜色。添加 / 编辑弹窗里有预览小方框和状态行（「已找到 180px」/「已找到 32px（偏小）· 点「查找高清图标」授权读网页再找一次」），改网址、改图标都会实时重算。

> 曾经的坑：编辑一个站点、什么都不改直接保存，图标会掉成首字色块——因为保存时是重建记录，把内部记的高清地址丢了；现在保存改成在旧记录上合并字段，不会再掉。

### 4.4 右上角的树叶

点一下，时钟、搜索框、快捷网址整层收起，只剩骑行画面；再点一下放回来。收起状态会记住——下次开新标签页还是收起的，而且因为是同步读取，**不会先闪一下再收起**。快捷键 <kbd>L</kbd> 同效。

收起之后树叶自己还在（颜色变淡，鼠标移上去恢复），不然就没法再点出来了。

> 收起之后，画面上剩下的操作只有底部那条控制条和键盘。想连控制条一起藏起来，按 <kbd>U</kbd>。

---

## 5. 底部的「快捷键」按钮

控制条第一行最右边（「晚上」右边）有个 <kbd>⌨</kbd> **快捷键** 按钮，鼠标移上去浮出一张表，**17 条**，<kbd>U</kbd> 显示 / 隐藏菜单排第一条。

- **想让它一直开着**：点一下按钮钉住（再点一下、按 <kbd>Esc</kbd>、或点面板外面收起）。松开鼠标也不消失，方便照着一条条按。
- **浮窗永远在屏幕里**：它贴在控制条右上角往左展开，而不是挂在按钮上——按钮会跟着控制条换行，粘在按钮上的话窗口一窄浮窗就从左边溢出屏幕。窗口很矮时列表自己变成可滚动的，也不会顶出屏幕。
- **最后两行**（<kbd>L</kbd> 收起 / 展开桌面层、<kbd>/</kbd> 聚焦搜索框）只在开了桌面层时才出现。
- **不想要这个按钮**：`config.js` 里把 `ui` 写成 `{ keys: false }`（见第 6 节）。

> 游戏页 `dist/index.html` 的完整 HUD 里没有这个按钮——右上角 <kbd>？</kbd> 打开的操作说明就是同一份内容。

---

## 6. 改时段 / 镜头 / 车速 / 画质 / 帧率

用记事本打开 **`dist/extension/config.js`**（很短），改完保存，然后回到 `chrome://extensions`，点这个插件的 **刷新图标**（或直接重启浏览器）即可生效：

```js
window.__PELICAN_WP = {
  enabled: true,   // false = 变成「可以玩」的新标签页（见下）
  tod: 'morning',  // 时段：morning 清晨 / day 白天 / dusk 傍晚 / night 晚上
  cam: 'chase',    // 镜头：chase 追随 / side 侧面跟拍 / cine 电影运镜 / pov 鹈鹕视角 / orbit 自由环绕
  kph: 6,          // 车速（公里/小时）。定速，以它为准，踏频随之自然变化
  cadence: 0,      // 目标踏频（rpm）。只在 kph 为 0 时生效；0 = 两者都不固定，用游戏默认
  q: 'high',       // 画质：low 流畅 / medium 均衡 / high 精美 / ultra 极致
  fps: 30,         // 锁帧，省电；设 0 表示不锁
  flow: false,     // true = 昼夜缓慢流动
  dayrate: 0.025   // 时间流速（时/秒），仅 flow 打开时有效：0.025 ≈ 16 分钟过完一天
};
/* 底部控制条：true = 可以拖速度、切时段、调海浪音量、做动作、换镜头、开关声音；false = 不显示
   也可以给对象按项裁剪，没写的项默认开，例如 { keys: false } 只藏「快捷键」按钮 */
window.__PELICAN_WP.ui = true;
/* 桌面层：时钟 + 农历、搜索框、快捷网址（右上角树叶可一键收起）。false = 不显示 */
window.__PELICAN_WP.home = true;
```

几个常用改法：

- **想要夜晚星空**：`tod: 'night'`；**傍晚暖橙**：`tod: 'dusk'`
- **笔记本怕费电**：`q: 'medium'` + `fps: 24`
- **4K 屏觉得糊**：`q: 'ultra'`（吃 GPU，注意散热）
- **走快一点 / 慢一点**：改 `kph`，例如 `12` 或 `2`（装了控制条以后，也可以直接拖滑块临时改）
- **想看镜头切换**：`cam: 'cine'`（8 个机位循环的电影运镜）
- **不要底部控制条**：把 `window.__PELICAN_WP.ui` 改成 `false`
- **只留一部分控件**：`ui` 也可以写成对象按项裁剪，如 `{ cams: false, sound: false }`（分别是速度滑块 / 时段 / 快捷键按钮 / 动作 / 镜头 / 声音六项：`speed` `tod` `keys` `acts` `cams` `sound`；`sound` 同时管「声音」按钮和「海浪」音量滑块）。**没写的项默认开**，所以只想藏一个就写 `{ keys: false }` 这样。
- **不要时钟和快捷网址**：把 `window.__PELICAN_WP.home` 改成 `false`；两层都设成 `false`，就是和桌面壁纸一模一样的纯画面
- **想在新标签页里玩完整游戏**：`enabled: false` —— 界面全部回来，开场遮罩、WASD 骑行、参数面板、音乐都能用，等于把游戏搬进了新标签页（此时控制条自动让位给完整 HUD）

> **车速和踏频只能定一个**：踏频 = 车速 ÷ 轮周长 × 齿比，只有车速能自由设。程序按 `kph` 定死车速，再自动挑一档、锁住变速，踏频随之确定。默认 6 km/h 挂在最低档约 **34 rpm**，腿会显得踩得偏慢（真实骑行到这个速度也是这样）；想要自然的 65 rpm 踏频，把 `kph` 改成 **11.5**。
>
> 想反过来定踏频（老写法）：把 `kph` 改成 `0`、`cadence` 填目标圈数即可，程序会反推车速。

---

## 7. 常见问题

**新标签页还是原来那个样子？**
插件没启用（到 `chrome://extensions` 看开关是否打开），或者同时装了别的新标签页插件——两个都支持覆盖新标签页时会互相抢，只能留一个。

**改了 `config.js` 没生效？**
浏览器缓存了扩展页面。在 `chrome://extensions` 点一下该插件的刷新按钮，或重启浏览器。

**画面卡在黑色 / 白屏？**
先双击 `dist/extension/newtab.html` 用 Chrome 打开：如果这样能正常显示，说明文件没问题，多半是硬件加速被关了——到 `chrome://settings/system` 打开「使用硬件加速」。

**底部控制条不想要 / 嫌挡画面？**
永久关掉：`config.js` 里把 `window.__PELICAN_WP.ui` 改成 `false`。临时藏起来：点一下页面，按 <kbd>U</kbd>，再按一次回来。

**控制条上的速度和键盘 W/S 会打架吗？**
不会。滑块定的是**巡航速度**——松开所有键以后会自动回到的那个速度；按住 <kbd>W</kbd>/<kbd>S</kbd> 是临时加速、刹车，松开就回到滑块设的值。

**快捷键记不住 / 都有哪些？**
控制条第一行最右边 <kbd>⌨</kbd> **快捷键** 按钮，鼠标悬浮就浮出全部 17 条；点一下钉住，慢慢照着按。详见第 5 节。

**鼠标移开浮窗就没了，能一直开着吗？**
能。点一下那个 <kbd>⌨</kbd> 按钮就钉住了，再点一下、按 <kbd>Esc</kbd>、或点面板外面才收起。

**在新标签页按 W/S/A/D 有用吗？**
有用，但要先点一下页面（焦点不在地址栏时按键才会到页面）。可用键：<kbd>W</kbd>/<kbd>S</kbd> 加减速、<kbd>A</kbd>/<kbd>D</kbd> 变道、<kbd>空格</kbd> 跳、<kbd>T</kbd> 特技、<kbd>B</kbd> 车铃、<kbd>H</kbd> 大叫、<kbd>C</kbd> 换镜头、<kbd>N</kbd> 换时段、<kbd>M</kbd> 音乐、<kbd>U</kbd> 隐藏界面。焦点在地址栏时按键归浏览器，不会和输入网址打架。

**会不会一开新标签页就突然出声？**
不会。声音默认关着（控制条上「声音」按钮不亮），点一下才启动音频，而且是音乐 + 海浪风声 + 音效一起开；切到别的标签页会自动静音。

**你们说的「参数面板」在哪？新标签页里怎么找不到？**
参数面板（<kbd>⚙️</kbd>，lil-gui 那套滑块）**只在游戏页 `dist/index.html` 里有**，点右上角工具条上的 <kbd>⚙️</kbd> 打开。壁纸版和新标签页里它是被刻意隐藏的——桌面上那个窗口连鼠标都不属于它，新标签页里则不需要一整套调参滑块。

所以插件里想调**海浪 / 风声的音量**，用底部控制条上「声音」按钮右边那个「**海浪**」滑块：往左拖小、拖到 0 就是静音；音乐和音效的总开关是它左边的「声音」按钮。

**背景音里时不时「噼里啪啦」一下？**
已在 1.2.2 修掉，两个原因都不是人耳能分辨的「某个音效」：

1. 跃出海面的鱼落水时的水花声——它是 400–2400 Hz 的宽带噪声，而背景层（海浪、风声、胎噪）全在 600 Hz 以下，于是成了整个背景里唯一的高频事件，每几秒响一次。现在**画面上的水花粒子照旧，声音去掉了**。
2. 音频节点泄漏——每响一个音符 / 音效都会新建几个节点接到常驻总线上，而代码里从不把它们摘下来，实测每分钟累积 250 个左右，挂机越久音频图越大。现在每个声音响完（`onended`）会自动断线，实测残留节点数稳定在常数级，不再随时间增长。

**会不会很费电？**
默认锁 30fps，比 60fps 省一半左右 GPU；画面自适应仍在（帧率持续偏低会自动降渲染分辨率，恢复后升回来）。只有新标签页在前台时才渲染。

**在搜索框里打字，鹈鹕会不会跟着动？**
不会。焦点落在输入框里时键盘完全归输入框，游戏不接管——只要是输入框、按钮或链接获得焦点，游戏就让开。所以在搜索框里打 `w` 就只是打出一个 `w`。

**快捷网址存在哪？换台电脑还有吗？**
存在这个扩展自己的本地存储里，不上传也不同步。换机器要么重新加一遍，要么把整个 `dist/extension` 目录拷过去。

**树叶点完找不到了？**
树叶一直都在右上角，收起后只是颜色变淡（鼠标移上去就恢复），不会消失。也可以按 <kbd>L</kbd> 键切换。

**农历会不会算错？**
不会。农历用的是浏览器内置的中国历法实现（`Intl` 的 `chinese` 日历），不自己维护月相表，闰月、大小月都由引擎判定。万一浏览器太老没有这个历法，日期行就退化成只显示公历 + 星期，不会硬凑一个错的农历出来。

**会不会泄露隐私 / 联网？**
插件本身不主动上传任何东西，安装时也没申请任何浏览器权限（连 `storage` 都没要）：画面、模型、纹理、音效全部本地实时生成，快捷网址和各项设置存在扩展自己的 localStorage 里，不上传、不读取你的历史记录或书签。

只有两种情况会联网，而且都是你主动触发的：一是点搜索或快捷网址，浏览器去访问那个网站；二是快捷网址的图标——浏览器会去**那个站点自己**的域名下试几组常见图标路径（不经过任何第三方图标服务），或者在你点「查找高清图标」并授权后，读一次该站点的首页 HTML 找它声明的图标。授权是可选权限，随时能在 `chrome://extensions` 里取消；不授权就只跑前几级。不想让它取图标，就在添加时自己填一个（emoji 或图片链接都行）。

**为什么插件目录里 JS 是分开的几个文件，壁纸版却是单个 HTML？**
Chrome 扩展页面的安全策略（CSP）是 `script-src 'self'`，**内联脚本会被直接拦掉**。所以插件版必须拆成独立文件：游戏本体是 `config.js` + `app.js`，桌面层是 `home-boot.js` + `home.js` + `home.css`。`config.js` 和 `home-boot.js` 都在 `<head>` 里同步加载，仍然在首帧之前执行，所以不会出现「先闪一下界面再隐藏」或者「先闪一下时钟再收起」的情况。壁纸版和游戏版是普通网页，没有这个限制，才做成单文件方便拖拽。

> 顺带一提：桌面层的三个文件只在构建插件时才会被写进产物——`dist/index.html` 和 `dist/wallpaper.html` 里搜不到它们。
